export interface UserInterface {
	fields: {
		label: string;
		inputType: string;
		inputName: string;
		inputPlaceholder: string;
		inputID: string;
		inputClasses: string;
	}[];
	buttonText: string;
	buttonClasses: string;
}
